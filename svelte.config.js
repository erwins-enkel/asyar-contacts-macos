// Present for svelte-check and editor tooling only — the build reads its
// Svelte options from `vite.config.ts`'s `svelte()` plugin. Both end up with
// the compiler defaults; this file exists because svelte-check refuses to run
// without a resolvable Svelte config.
export default {};
